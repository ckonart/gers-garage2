spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gers-garage2
    username: postgres
    password: Caio6021
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
  thymeleaf:
    cache: false
    enabled: true
    prefix: classpath:/templates/
    suffix: .html
